# --- Wallet / Identity (only public addresses here; DO NOT store private keys) ---
METAMASK_ADDRESS=0xYOUR_METAMASK_ADDRESS_HERE
MINER_WALLET=0xYOUR_MINER_WALLET_ADDRESS_HERE
WORKER_NAME=hsm-worker-01

# --- Mining / Pool --- 
POOL_URL=stratum+tcp://your-pool.example:3333        # or blank for offline/sim
POOL_USER=${MINER_WALLET}.${WORKER_NAME}
POOL_PASS=x

# --- HSM / HSM-Engine / Ledger ---
LEDGER_DIR=./hs_ledger
HS_ALERT_WEBHOOK=
SIMULATION_MODE=1              # 1 = safe/offline simulation; 0 = live

# --- Runtime / Performance ---
NUM_INSTANCES=2                # number of concurrent miner processes (gpu scaler overrides)
GPU_DEVICE_INDEX=0
GPU_MEM_PER_INSTANCE_MB=1024   # scaler uses this to estimate instances

# --- Logging / Paths ---
LOG_DIR=./logs
TMP_DIR=./tmp

# --- Network / Telemetry (optional) ---
METRICS_ENABLED=0
METRICS_PORT=9000

# --- Optional API keys (leave blank if unused; rotate regularly) ---
ETHERSCAN_API_KEY=
OTHER_API_KEY=

# --- Safety / Admin ---
ADMIN_EMAIL=your.team@example.com
SIMULATION_MODE=1
